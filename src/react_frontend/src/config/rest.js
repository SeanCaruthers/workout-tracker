const base = "http://localhost:3000";
const exercises_endpoint = "exercises";

const urls = {
    base: base,
    exercises: {
        endpoint: `${base}/${exercises_endpoint}`
    }
}

export default urls;